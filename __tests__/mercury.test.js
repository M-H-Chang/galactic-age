/* eslint-disable no-undef */
import Mercury from './../src/mercury.js';

describe('Mercury', () => {
  test('this should divide 5 by .24', () => {
    divideByMercury(5);
    expect(num.merc).toEqual(21);
  });
});